<% if error.errors.any? %>
  <div id="error_explanation">
    <h3><%= pluralize(error.errors.count, "error") %> prohibited a successful save:</h3>

    <ul>
      <% error.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>